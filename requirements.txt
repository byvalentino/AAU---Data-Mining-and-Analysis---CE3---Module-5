# Pinned after a green build, never guessed. One caveat, and it is the reason
# check 1b (a clean-machine install) is still unsigned in this module:
#
#   Every number in this module was measured with pyarrow 18.1.0, which is what
#   the other three modules pin. mlflow 2.17.2 declares pyarrow<18, so a clean
#   `pip install -r requirements.txt` cannot reproduce that combination -- pip
#   refuses to resolve it. 17.0.0 is pinned here because it is the pair that
#   installs. Parquet round-trips of float and integer columns are identical
#   between the two, and the manifest hashes values rather than bytes, so
#   nothing measured is expected to move; expected is not verified.
#
#   Settle it once, on a machine where installation works:
#       bash setup.sh && make check          # expect data 0, labs 2 2 2 2
#       python3 apply.py && make check       # expect 0 0 0 0
#   If anything moves, the alternative is mlflow==2.18.0, which allows
#   pyarrow<19 and so admits 18.1.0 -- untested here for the same reason.
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
scikit-learn==1.5.2
pyarrow==17.0.0
pytest==8.3.4
plotly==5.24.1
kaleido==0.2.1
mlflow==2.17.2
