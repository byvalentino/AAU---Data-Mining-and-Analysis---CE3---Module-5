# Pinned after a green build, never guessed.
#
# This module logs a plain scikit-learn model, so MLflow 3's change of default
# serialisation format does not touch it -- unlike Module 3, whose pipeline
# carries a class written for this course and must name cloudpickle explicitly.
# If a custom step is ever added here, that call needs the same parameter.
#
# The move to MLflow 3 is what frees pyarrow. MLflow 2.17.2 required
# pyarrow<18, which is why this module used to pin 17.0.0 while Modules 1, 2
# and 4 pinned 18.1.0 -- one course, two versions of the same reader. MLflow
# 3.15.1 asks for pyarrow>=4.0.0,<26, so all five modules now pin 18.1.0.
#
# Verified: Module 5 reaches four green checks on MLflow 3.15.1, unchanged.
# Not verified on this machine: the exact tuple below on Python 3.11, because
# pyarrow 18.1.0 has no wheel for macOS x86_64 there. Settle it once in the
# devcontainer:
#       bash setup.sh && make check          # expect data 0, labs 2 2 2 2
#       python3 apply.py && make check       # expect 0 0 0 0
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
scikit-learn==1.5.2
pyarrow==18.1.0
pytest==8.3.4
plotly==5.24.1
kaleido==0.2.1
mlflow==3.15.1
